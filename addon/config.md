# Attention Is All You Need

- `managedDecks`：计入全局注意力预算的根牌组。子牌组自动包含。
- `dashboardPort`：本地看板首选端口；占用时自动选择空闲端口。
- `simulationDays`：FSRS 模拟周期。
- `dailyBudgetMinutes`：每日 Anki 注意力预算。

看板只监听 `127.0.0.1`。暂停、标签等写操作尚未开放。
